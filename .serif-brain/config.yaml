# serif-brain config — auto-generated by 'serif-brain init'
# Edit freely. Re-running init will not overwrite this file.

schema_version: "1.0"
created_at: "2026-05-08T07:43:53.492Z"
project_root: "/Users/muhammedserifdemir/Desktop/seriftech-packages/serif-brain-core"

store:
  engine: node:sqlite     # node:sqlite | jsonl
  db_path: brain.db
  jsonl_dir: objects-jsonl
  node_version: "24.11.1"

# NOT: bu brain 2026-05-08'de, init'in eski (isimsiz kurulumda sabit 4 proje yazan)
# surumuyle kuruldu; bu yuzden AKTIF proje serif-platform gorunuyor, oysa buradaki
# 8 objenin hepsi seriftech-packages altinda. 'add' bu yuzden yanlis dizine yaziyordu.
# init 13919b3'te duzeldi, bu dosya geride kaldi — 2026-08-11'de elle duzeltildi.
projects:
  - id: seriftech-packages
    active: true
    migrate: false
    description: "serif-brain-core — paketin kendi hafizasi (dogfood)"

# serif-brain-core'un KENDI modulleri (src/<modul>). Eskiden buraya SerifX360'in
# uygulama isimleri (contentx/presentx/...) yaziliydi — hicbiri bu repoda yok,
# bu yuzden 8 objenin hepsi 'infra'ya dusmustu.
valid_modules:
  - cli
  - query
  - graph
  - scanner
  - markdown
  - mcp
  - dashboard
  - context
  - reporter
  - skills
  - hooks
  - test
  - docs
  - infra
  - unknown

# Dosya yolu -> modul. graph build / scan code / impact / guard bunu okur.
module_paths:
  "src/cli/": cli
  "src/query/": query
  "src/graph/": graph
  "src/scanner/": scanner
  "src/markdown/": markdown
  "src/mcp/": mcp
  "src/dashboard/": dashboard
  "src/context/": context
  "src/reporter/": reporter
  "src/skills/": skills
  "src/ingest/": markdown
  "src/migrate/": markdown
  "src/store/": markdown
  "src/util/": infra
  "src/doctor/": infra
  "src/hooks/": hooks
  "hooks/": hooks
  "bin/": cli
  "test/": test
  "docs/": docs
  "skill/": skills

valid_status:
  - queued
  - open
  - active
  - in_progress
  - blocked
  - done
  - rejected
  - archived

valid_priority:
  - critical
  - high
  - medium
  - low

# Context compiler will exclude records with these statuses from Claude context
context_excluded_status:
  - done
  - rejected
  - archived

# Status normalization (legacy -> canonical) — applied during ingest/migration
status_normalization:
  completed: done
  closed: done
  planned: queued

# Module normalization
module_normalization:
  testlms: testx
  TestLMS: testx
  TestX: testx
  PresentX: presentx
  ContentX: contentx
  AnimatorX: animatorx
  StudioX: studiox

# Sources to read during migration (Faz 5). Read-only references.
legacy_sources:
  archive_root: "/Users/muhammedserifdemir/SerifBrainArchive/legacy-2026-04-29-230615"
  read_from_archive: true     # always read from archive, not live sources
